<?xml version="1.0" encoding="utf-8"?>
<!-- Edited: 2026-01-06 -->
<!-- Purpose: Android manifest; declare permissions, app config, and MainActivity. -->
<!-- AndroidManifest -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network and location permissions for sync and location updates -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Android 13+ notifications permission (safe to declare; runtime requested by UI/worker) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />

    <!-- Declare location hardware feature as not required -->
    <uses-feature
        android:name="android.hardware.location"
        android:required="false" />

    <application
        android:name=".CeylonQueueBusPulseApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CeylonQueueBusPulse"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="${ALLOW_CLEARTEXT}"
        tools:targetApi="31">

        <!-- TomTom SDK key (set TOMTOM_API_KEY in gradle.properties or local.properties to populate this) -->
        <meta-data android:name="com.tomtom.sdk.key" android:value="${TOMTOM_SDK_KEY}" />

        <!-- Disable automatic WorkManager initialization (we use Koin's workManagerFactory) -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CeylonQueueBusPulse">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep links: open to a route or report -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ceylonqueue" android:host="route" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ceylonqueue" android:host="report" />
            </intent-filter>
        </activity>

        <!-- Compose-based Map activity -->
        <activity
            android:name=".traffic.MapComposeActivity"
            android:exported="false">
            <!-- Map should be opened internally after login. Deep links are handled by MainActivity. -->
        </activity>

        <!-- Settings screen -->
        <activity android:name=".ui.SettingsActivity" android:exported="false" />

        <!-- Compose Traffic Detail screen -->
        <activity android:name=".ui.TrafficDetailActivity" android:exported="false" />

        <!-- Map activity for TomTom map interactions (legacy) -->
        <activity android:name=".traffic.MapActivity" android:exported="false" />

        <!-- Privacy & permissions screen -->
        <activity android:name=".ui.PrivacyPolicyActivity" android:exported="false" />


    </application>


</manifest>